
class Solution:
    def longest(self, arr):
        max =arr[0]
        max_len = len(max)
        for i in arr:
            if len(i)>max_len:
                max_len = len(i)
                max = i
        return max
            
            
       
